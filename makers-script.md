# Makers 번역 파이프라인 — 스크립트 명세

이 문서를 그대로 코딩 모델에게 주고 스크립트를 작성하게 한다.

---

## 목적

영문 소설 조각 48개를 순차로 번역 API에 보내고, 결과를 조각별 파일로 저장한다.
중단 후 재실행하면 이미 끝난 조각은 건너뛰고 이어서 진행한다.

---

## 디렉토리 구조

```
makers/
├── translate.py          ← 작성할 스크립트
├── 방침.md                ← 번역 방침 (프롬프트에 매번 삽입)
├── chunks/               ← 입력. 001.txt ~ 048.txt
└── out/                  ← 출력. 001.txt ~ 048.txt (스크립트가 생성)
```

---

## 동작

1. `방침.md` 전문을 읽어 메모리에 올린다.
2. `chunks/` 의 `.txt` 파일을 파일명 번호순으로 정렬한다.
3. 각 조각에 대해:
   - `out/<번호>.txt` 가 이미 존재하고 내용이 비어 있지 않으면 **건너뛴다**
   - 직전 번호의 `out/` 결과가 있으면 그 **마지막 3문단**을 읽어 문맥으로 쓴다 (없으면 생략)
   - 아래 프롬프트를 구성해 API 호출
   - 응답을 `out/<번호>.txt` 에 UTF-8로 저장
   - 다음 조각으로
4. 진행 상황을 콘솔에 출력한다: `[12/48] 012.txt 처리 중... 완료 (4,832자, 41.2초)`

---

## 프롬프트 구성

system:
```
당신은 문학 번역가입니다. 아래 번역 방침을 엄격히 따라 영문 소설을
한국어로 옮깁니다. 번역문만 출력하고, 설명이나 해설은 붙이지 않습니다.
```

user (아래를 이 순서로 이어붙임):
```
# 번역 방침

<방침.md 전문>

---

# 직전 대목의 끝부분 (문맥 참고용, 번역하지 말 것)

<직전 out/ 결과의 마지막 3문단>

---

# 번역할 원문

조각 전체를 한 번에 번역하십시오. 장면 단위로 끊지 마십시오.

<이번 조각 원문 전문>
```

첫 조각이거나 직전 결과가 없으면 "직전 대목" 블록 전체를 생략한다.

---

## API 호출

- Anthropic Messages API 사용
- 모델은 상수로 빼서 쉽게 교체 가능하게
- `max_tokens` 는 넉넉히 (조각당 한국어 출력이 8,000토큰을 넘을 수 있음)
- API 키는 환경변수 `ANTHROPIC_API_KEY` 에서 읽는다. 코드에 하드코딩 금지

---

## 오류 처리

- 실패 시 최대 3회 재시도, 지수 백오프 (2초 → 4초 → 8초)
- 3회 모두 실패하면 해당 조각을 건너뛰고 `failed.log` 에 번호를 기록한 뒤 다음으로 진행.
  **전체 실행을 중단하지 말 것**
- 429(rate limit) 응답은 `Retry-After` 헤더가 있으면 그만큼 대기
- 응답이 비어 있거나 100자 미만이면 실패로 간주하고 재시도
- Ctrl+C 로 중단해도 이미 저장된 파일은 그대로 남아야 한다

---

## 기타

- 요청 사이에 3초 대기
- 시작 시 전체 조각 수와 이미 완료된 수를 출력
- 종료 시 요약 출력: 처리한 조각 수, 건너뛴 수, 실패한 수, 총 소요 시간
- `--only 12` 옵션: 특정 조각 하나만 (재)처리. 기존 결과가 있어도 덮어씀
- `--range 10-20` 옵션: 범위 지정 처리
- `--dry-run` 옵션: API 호출 없이 어떤 조각이 처리될지만 출력

---

## 별도 스크립트: 합치기

`merge.py` — `out/` 의 결과를 번호순으로 이어붙여 한 파일로 만든다.

- 조각 사이에 빈 줄 두 개
- 맨 앞에 아래 고지를 붙인다:

```
Makers — Cory Doctorow
원문: https://craphound.com/makers/
라이선스: CC BY-NC-SA 3.0 US
이 번역문 역시 동일한 조건으로 배포됩니다.
```

- 출력은 `makers-ko.md`
- 표지 이미지는 넣지 않는다 (라이선스상 금지)
